apiVersion: v1
kind: Service
metadata:
  name: flask-basic-app
  namespace: flask-app
spec:
  type: LoadBalancer
  selector:
    app: flask-basic-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5007
